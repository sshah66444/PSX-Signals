# Legacy workflow disabled when the next-session briefing is installed.
# The new schedule is in next_day_briefing.yml.
name: Legacy PSX Scan (Disabled)
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use PSX Next-Trading-Day Briefing instead.'
